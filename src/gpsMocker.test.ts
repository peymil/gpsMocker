import { lngDDM2DD, lngDD2DDM, ltdDD2DDM, ltdDDM2DD } from "./gpsMocker";

it("It should parse DDM (Degrees Decimal Minutes) longitude to DD (Decimal Degrees)", () => {});
it("It should parse DD (Decimal Degrees) longitude to DDM (Degrees Decimal Minutes)", () => {});
it("It should parse DDM (Degrees Decimal Minutes) latitude to DD (Decimal Degrees)", () => {});
it("It should parse DD (Decimal Degrees) latitude to DDM (Degrees Decimal Minutes)", () => {});
